{{!-- This is for the photos inside chosen album --}}

<div class="pic-box">
  <div class="photo">
    <img src="{{photo_url}}" alt="{{description}}">
  </div>
  <div><h4>{{album}}</h4></div>
  <div><p>{{description}}</p></div>
</div>